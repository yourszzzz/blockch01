# blockch01
// read functions are free in blockchain while write costs a gas fee.
to start local block chain - 'npx hardhat node'
to deploy in other terminal - npx hardhat run scripts/deploy-counter.js --network localhost
to interact in console - npx hardhat console --network localhost
